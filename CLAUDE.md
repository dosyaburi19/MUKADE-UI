# 프로젝트 개요

이 프로젝트는 Svelte UI 라이브러리입니다.
기본적인 웹 메신저 UI 를 만들기 위해서 만들어진 Svelte UI 라이브러리입니다.

# 기술 스택

- Svelte & SvelteKit, TypeScript

# 개발 명령어

- 실행: npm run dev

# 코딩 컨벤션

- 최소한으로 svelte 만들 때의 라이브러리 외에는 다른 라이브러리를 install 하지 않고, 의존성을 최소화시킴.

# 해당 프로젝트의 목표

- React 의 MUI 같은 대중적인 라이브러리를 목표로 Svelte 를 대표하는 UI Library 를 만들고자하는 방향이 있음.
- 심플하고 특유의 터미널 같은 디자인을 추구함.

# 주의사항

- "직접 수정해줘" 라는 말이 있지 않는 이상 Claude Code 는 어떠한 상황에서도 직접 수정하지 않음